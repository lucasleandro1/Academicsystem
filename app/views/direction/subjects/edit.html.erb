<% content_for :title, "Editar Disciplina" %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1><i class="fas fa-edit"></i> Editar Disciplina</h1>
  <div>
    <%= link_to direction_subject_path(@subject), class: "btn btn-outline-secondary me-2" do %>
      <i class="fas fa-eye"></i> Ver Detalhes
    <% end %>
    <%= link_to direction_subjects_path, class: "btn btn-outline-secondary" do %>
      <i class="fas fa-arrow-left"></i> Voltar
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col-lg-8 mx-auto">
    <%= form_with model: [:direction, @subject], local: true, class: "needs-validation", novalidate: true do |form| %>
      
      <!-- Informações Básicas -->
      <div class="card mb-4">
        <div class="card-header">
          <h5 class="mb-0"><i class="fas fa-info-circle"></i> Informações Básicas</h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-8 mb-3">
              <%= form.label :name, "Nome da Disciplina *", class: "form-label" %>
              <%= form.text_field :name, class: "form-control", 
                                 placeholder: "Ex: Matemática, Língua Portuguesa, História", required: true %>
              <div class="invalid-feedback">
                Por favor, informe o nome da disciplina.
              </div>
              <div class="form-text">
                Nome completo da disciplina
              </div>
            </div>

            <div class="col-md-4 mb-3">
              <%= form.label :code, "Código da Disciplina", class: "form-label" %>
              <%= form.text_field :code, class: "form-control", placeholder: "Ex: MAT, PORT, HIST" %>
              <div class="form-text">
                Código único para identificação
              </div>
            </div>

            <div class="col-md-6 mb-3">
              <%= form.label :area, "Área do Conhecimento *", class: "form-label" %>
              <%= form.select :area, 
                              options_for_select([
                                ['Selecione...', ''],
                                ['Linguagens', 'languages'],
                                ['Matemática', 'mathematics'],
                                ['Ciências da Natureza', 'natural_sciences'],
                                ['Ciências Humanas', 'human_sciences'],
                                ['Ensino Religioso', 'religious_education'],
                                ['Educação Física', 'physical_education'],
                                ['Artes', 'arts'],
                                ['Tecnologia', 'technology']
                              ], @subject.area),
                              {}, { class: "form-select", required: true } %>
              <div class="invalid-feedback">
                Por favor, selecione a área do conhecimento.
              </div>
            </div>

            <div class="col-md-6 mb-3">
              <%= form.label :workload, "Carga Horária Semanal *", class: "form-label" %>
              <div class="input-group">
                <%= form.number_field :workload, class: "form-control", min: 1, max: 40, required: true %>
                <span class="input-group-text">horas/semana</span>
              </div>
              <div class="invalid-feedback">
                Por favor, informe a carga horária.
              </div>
              <div class="form-text">
                Número de aulas por semana
              </div>
            </div>

            <div class="col-12 mb-3">
              <%= form.label :description, "Descrição da Disciplina", class: "form-label" %>
              <%= form.text_area :description, class: "form-control", rows: 4, 
                                placeholder: "Descrição dos objetivos, conteúdos e metodologia da disciplina..." %>
              <div class="form-text">
                Informações detalhadas sobre a disciplina
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Atribuições -->
      <div class="card mb-4">
        <div class="card-header">
          <h5 class="mb-0"><i class="fas fa-users"></i> Atribuições</h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-6 mb-3">
              <%= form.label :classroom_id, "Turma *", class: "form-label" %>
              <%= form.select :classroom_id, 
                              options_from_collection_for_select(@classrooms, :id, :name, @subject.classroom_id),
                              { prompt: "Selecione uma turma..." }, 
                              { class: "form-select", required: true } %>
              <div class="invalid-feedback">
                Por favor, selecione uma turma.
              </div>
            </div>

            <div class="col-md-6 mb-3">
              <%= form.label :user_id, "Professor Responsável", class: "form-label" %>
              <%= form.select :user_id, 
                              options_from_collection_for_select(@teachers, :id, :full_name, @subject.user_id),
                              { prompt: "Selecione um professor..." }, 
                              { class: "form-select" } %>
              <div class="form-text">
                Professor que irá lecionar esta disciplina
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Status e Configurações -->
      <div class="card mb-4">
        <div class="card-header">
          <h5 class="mb-0"><i class="fas fa-cog"></i> Status e Configurações</h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-6 mb-3">
              <div class="form-check form-switch">
                <%= form.check_box :active, class: "form-check-input" %>
                <%= form.label :active, "Disciplina Ativa", class: "form-check-label" %>
              </div>
              <div class="form-text">
                Disciplinas inativas não aparecem nos horários e relatórios
              </div>
            </div>

            <div class="col-md-6 mb-3">
              <div class="form-check form-switch">
                <%= form.check_box :allows_makeup_exams, class: "form-check-input" %>
                <%= form.label :allows_makeup_exams, "Permite Recuperação", class: "form-check-label" %>
              </div>
              <div class="form-text">
                Permite provas e atividades de recuperação
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Botões de Ação -->
      <div class="card">
        <div class="card-body">
          <div class="d-flex justify-content-between">
            <div>
              <%= link_to direction_subject_path(@subject), 
                         class: "btn btn-danger", 
                         method: :delete,
                         data: { 
                           confirm: "Tem certeza que deseja excluir esta disciplina? Esta ação não pode ser desfeita e removerá todas as atividades, notas e horários relacionados." 
                         } do %>
                <i class="fas fa-trash"></i> Excluir Disciplina
              <% end %>
            </div>
            
            <div>
              <%= link_to direction_subjects_path, class: "btn btn-secondary me-2" do %>
                <i class="fas fa-times"></i> Cancelar
              <% end %>
              <%= form.submit "Salvar Alterações", class: "btn btn-primary" do %>
                <i class="fas fa-save"></i> Salvar Alterações
              <% end %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<!-- Informações Adicionais -->
<div class="row mt-4">
  <div class="col-lg-8 mx-auto">
    <div class="alert alert-info">
      <h6><i class="fas fa-info-circle"></i> Informações Importantes:</h6>
      <ul class="mb-0">
        <li>Alterações no nome ou código da disciplina podem afetar relatórios históricos</li>
        <li>Mudanças na turma moverão todas as atividades e notas para a nova turma</li>
        <li>Remover o professor responsável pode afetar as funcionalidades do sistema</li>
        <li>Disciplinas inativas não aparecem para alunos e professores</li>
      </ul>
    </div>
  </div>
</div>

<script>
// Validação customizada do formulário
(function() {
  'use strict';
  window.addEventListener('load', function() {
    var forms = document.getElementsByClassName('needs-validation');
    var validation = Array.prototype.filter.call(forms, function(form) {
      form.addEventListener('submit', function(event) {
        if (form.checkValidity() === false) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  }, false);
})();
</script>
